# Take user input, separate digits assign the correct type and add the digits to get the sum
two_digit_number = input("Enter a two digit number here: \n") #Prompt user for input

print(two_digit_number[0])
print(two_digit_number[1])

first_digit = int(two_digit_number[0])                         #convert string to integer
second_digit = int(two_digit_number[1])                        
result = print(first_digit + second_digit)                     #Add integers
